# <Flow Name>

**Spec file:** [`tests/<path>.spec.ts`](../tests/<path>.spec.ts)
**Project(s):** `chromium`, `firefox`, `webkit` <!-- or `unauthenticated`, `setup`, etc. -->
**Preconditions:** <!-- e.g. "Authenticated (reuses storage state from the `setup` project)" or "None — starts logged out" -->

## Purpose

<!-- One or two sentences: what user flow this covers and why it matters. -->

## Scenarios

- **<scenario name>** — <action> → <expected outcome>.
- **<scenario name>** — <action> → <expected outcome>.
